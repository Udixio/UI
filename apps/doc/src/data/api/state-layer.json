{
  "schemaVersion": 3,
  "displayName": "StateLayer",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/StateLayer.tsx",
      "description": "Paints the Material 3 state layer over its trigger, and drives the press\nripple.",
      "tags": {
        "status": "beta",
        "category": "Interaction",
        "devx": "- Building block used by every interactive component (`Button`, `Chip`, `Switch`, ...); it is\n  rendered inside the element it decorates, never standalone.\n- `colorName` is a theme token without the `--color-` prefix, and it is the\n  content colour of the surface being decorated -- `on-primary` on a filled\n  button, `on-primary-container` once that button is repainted onto a\n  container. An unknown token degrades to `on-surface`.\n- `stateClassName` selects the Tailwind utility driving the CSS states. The\n  `state-ripple-group-[name]` form pairs with a `group/name` class on the\n  trigger, and the layer attaches its ripple to that same element, so the\n  CSS states and the JavaScript gesture always agree on what the trigger is.\n- `state-layer` (the non-group form) is CSS-only: no ripple is wired.",
        "a11y": "- Decorative only: the layer carries `aria-hidden` and takes no pointer\n  events, so it never reaches the accessibility tree nor intercepts input.\n- The press ripple honours the reduced-motion preference through the shared\n  controller.",
        "limitations": "- The trigger is resolved by walking up to the outermost ancestor carrying\n  the named Tailwind group, so the layer must be rendered inside it.\n- The set of valid `colorName` tokens lives in `@udixio/theme`, which\n  `@udixio/core` does not depend on, so the prop is typed as `string`.\n- `className` accepts the state-aware function form, but this contract\n  resolves no interaction states: the function receives the props and an\n  empty state object, because hover, focus and press live entirely in the\n  Tailwind utilities rather than in JavaScript."
      },
      "methods": [],
      "props": {
        "colorName": {
          "name": "colorName",
          "description": "Semantic color token name, without the `--color-` prefix -- `on-primary`,\n`on-surface-variant`, and so on.\n\nAn unknown name falls back to `on-surface` rather than resolving to\nnothing: the Tailwind hover and focus utilities read `--state-color`\nwithout a fallback of their own, so an unresolvable token used to remove\nthe hover state entirely while leaving the ripple visible.\n\nThis is typed as `string` because the set of valid tokens lives in\n`@udixio/theme`, which `@udixio/core` does not depend on.",
          "required": true,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "stateClassName": {
          "name": "stateClassName",
          "description": "Tailwind state utility selecting the interaction trigger.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": {
            "value": "'state-ripple-group'"
          }
        },
        "shapeTransition": {
          "name": "shapeTransition",
          "description": "Optional Motion animation applied to the state layer trigger.",
          "required": false,
          "type": {
            "name": "StateLayerShapeTransition"
          },
          "defaultValue": null
        },
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<StateLayerInterface>"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/state-layer/state-layer.ts",
      "description": "Paints the Material 3 state layer over its trigger, and drives the press\nripple.",
      "selector": "udx-state-layer",
      "tags": {
        "status": "beta",
        "category": "Interaction",
        "devx": "- Building block used by every interactive component (`Button`, `Chip`, `Switch`, ...); it is\n  rendered inside the element it decorates, never standalone.\n- `colorName` is a theme token without the `--color-` prefix, and it is the\n  content colour of the surface being decorated -- `on-primary` on a filled\n  button, `on-primary-container` once that button is repainted onto a\n  container. An unknown token degrades to `on-surface`.\n- `stateClassName` selects the Tailwind utility driving the CSS states. The\n  `state-ripple-group-[name]` form pairs with a `group/name` class on the\n  trigger, and the layer attaches its ripple to that same element, so the\n  CSS states and the JavaScript gesture always agree on what the trigger is.\n- `state-layer` (the non-group form) is CSS-only: no ripple is wired.",
        "a11y": "- Decorative only: the layer carries `aria-hidden` and takes no pointer\n  events, so it never reaches the accessibility tree nor intercepts input.\n- The press ripple honours the reduced-motion preference through the shared\n  controller.",
        "limitations": "- The trigger is resolved by walking up to the outermost ancestor carrying\n  the named Tailwind group, so the layer must be rendered inside it. An\n  attribute directive on that ancestor would make the CSS group and the\n  JavaScript trigger the same element by construction rather than by search.\n  That shape was considered and declined: unlike a component asking for a\n  reference to an element it does not own, this one is rendered inside its\n  trigger, and the walk crosses only this component's own `display: contents`\n  host before reaching the group. Half of its call sites render it from an\n  `ng-template` projected into two or three different hosts, so the\n  conversion would restructure those templates and turn their render\n  conditions into inert bindings -- a large change to interaction feedback\n  for a walk that is correct and covered by tests. Revisit it if the group\n  ever stops being an ancestor of the layer.\n- The set of valid `colorName` tokens lives in `@udixio/theme`, which\n  `@udixio/core` does not depend on, so the prop is typed as `string`.\n- `className` accepts the state-aware function form, but this contract\n  resolves no interaction states: the function receives the props and an\n  empty state object, because hover, focus and press live entirely in the\n  Tailwind utilities rather than in JavaScript."
      },
      "inputs": {
        "colorName": {
          "name": "colorName",
          "description": "Semantic color token name, without the `--color-` prefix -- `on-primary`,\n`on-surface-variant`, and so on.\n\nAn unknown name falls back to `on-surface` rather than resolving to\nnothing: the Tailwind hover and focus utilities read `--state-color`\nwithout a fallback of their own, so an unresolvable token used to remove\nthe hover state entirely while leaving the ripple visible.\n\nThis is typed as `string` because the set of valid tokens lives in\n`@udixio/theme`, which `@udixio/core` does not depend on.",
          "required": true,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "stateClassName": {
          "name": "stateClassName",
          "description": "Tailwind state utility selecting the interaction trigger.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": {
            "value": "'state-ripple-group'"
          }
        },
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<StateLayerInterface>"
          },
          "defaultValue": null
        },
        "shapeTransition": {
          "name": "shapeTransition",
          "description": "Optional Motion animation applied to the state layer trigger.",
          "required": false,
          "type": {
            "name": "StateLayerShapeTransition | undefined"
          },
          "defaultValue": null
        }
      },
      "outputs": {}
    }
  }
}
