{
  "schemaVersion": 4,
  "displayName": "TabPanel",
  "description": "A tab panel holds the content shown for a single tab.",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/TabPanel.tsx",
      "tags": {
        "status": "beta",
        "parent": "Tabs",
        "category": "Navigation",
        "devx": "- Must be rendered inside a `TabPanels`, itself inside a `TabGroup`.",
        "a11y": "- Exposes `role=\"tabpanel\"`, an `id`/`aria-labelledby` pair matching the\n  connected `Tab`, and `tabIndex={0}` so keyboard users can move focus\n  into the panel content.",
        "limitations": "- Unmounts when it stops being the active panel; scroll position, form\n  input, and focus inside it are not preserved across a switch."
      },
      "methods": [],
      "props": {
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<TabPanelInterface>"
          },
          "defaultValue": null
        },
        "index": {
          "name": "index",
          "description": "Injected by the parent TabPanels: position of this panel in the list.",
          "required": false,
          "type": {
            "name": "number"
          },
          "defaultValue": null
        },
        "tabsId": {
          "name": "tabsId",
          "description": "Injected by the parent TabPanels: identifier shared by every tab and panel of the group.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/tabs/tab-panel.ts",
      "selector": "udx-tab-panel",
      "tags": {
        "status": "beta",
        "category": "Navigation",
        "parent": "Tabs",
        "devx": "- Must be rendered inside a `udx-tab-panels`, itself inside a `udx-tab-group`.",
        "a11y": "- Exposes `role=\"tabpanel\"`, an `id`/`aria-labelledby` pair matching the\n  connected `udx-tab`, and `tabIndex={0}` while active so keyboard users\n  can move focus into the panel content.",
        "limitations": "- Unlike React, scroll position and focus inside an inactive panel are\n  preserved (its component instance stays alive, only hidden) -- do not\n  rely on either behavior for cross-framework parity."
      },
      "inputs": {
        "classes": {
          "name": "classes",
          "description": "Classes, or state-aware element classes, applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<TabPanelInterface>"
          },
          "defaultValue": null
        }
      },
      "outputs": {},
      "content": {
        "default": {
          "name": "default",
          "selector": "*",
          "description": "Default projected content."
        }
      }
    }
  }
}
