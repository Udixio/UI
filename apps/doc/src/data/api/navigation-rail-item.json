{
  "schemaVersion": 4,
  "displayName": "NavigationRailItem",
  "description": "Navigation rail items are the destinations a navigation rail switches between.",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/NavigationRailItem.tsx",
      "tags": {
        "status": "beta",
        "parent": "NavigationRail",
        "devx": "- Selection is index-based and provided by the parent rail.\n- `extendedOnly` hides items when the rail is collapsed.\n- `badge` puts a `Badge` on the icon, the way Material shows notifications\n  on a destination; pass `undefined` once the destination is selected if\n  the notification is meant to clear, and the badge animates out.\n- The label reveal (width/height + opacity, on `extended` changes) is\n  driven by a shared `@udixio/core/dom` Motion controller, the same one\n  the Angular adapter uses.",
        "a11y": "- Exposes `aria-current=\"page\"` when selected, since the rail is a\n  navigation landmark rather than a tabbed panel switcher.",
        "limitations": "- No arrow-key navigation between items; relies on the native sequential\n  tab order (a roving tabindex is only required for `tablist`/`listbox`\n  widgets, which this is not)."
      },
      "methods": [],
      "props": {
        "className": {
          "name": "className",
          "description": "Root classes (string), static element classes (object), or state-aware element classes (function).",
          "required": false,
          "type": {
            "name": "string | Partial<Record<\"container\" | \"label\" | \"stateLayer\" | \"icon\" | \"navigationRailItem\", string>> | ClassNameComponent<NavigationRailItemInterface>"
          },
          "defaultValue": null
        },
        "badge": {
          "name": "badge",
          "description": "A badge on the icon, the way Material 3 shows notifications on a\ndestination: `{}` is the small dot, `{ label: 3 }` the count. Material\nrecommends removing it once the destination has been viewed, which is\nthe caller's call -- pass `undefined` when the item is selected.",
          "required": false,
          "type": {
            "name": "BadgeProps"
          },
          "defaultValue": null
        },
        "selected": {
          "name": "selected",
          "description": "Controlled selected state, used when no parent drives the selection.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "false"
          }
        },
        "variant": {
          "name": "variant",
          "description": "Injected by the parent NavigationRail: layout direction of the item.",
          "required": false,
          "type": {
            "name": "\"vertical\" | \"horizontal\""
          },
          "defaultValue": {
            "value": "'vertical'"
          }
        },
        "index": {
          "name": "index",
          "description": "Injected by the parent NavigationRail: position of this item in the rail.",
          "required": false,
          "type": {
            "name": "number"
          },
          "defaultValue": null
        },
        "selectedItem": {
          "name": "selectedItem",
          "description": "Injected by the parent NavigationRail: index of the currently selected item.",
          "required": false,
          "type": {
            "name": "number"
          },
          "defaultValue": null
        },
        "isExtended": {
          "name": "isExtended",
          "description": "Injected by the parent NavigationRail: whether the rail is extended.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": null
        },
        "extendedOnly": {
          "name": "extendedOnly",
          "description": "Injected by the parent NavigationRail: hide the item while the rail is collapsed.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/navigation-rail/navigation-rail-item.ts",
      "selector": "udx-navigation-rail-item",
      "tags": {
        "status": "beta",
        "parent": "NavigationRail",
        "devx": "- Selection is index-based and resolved from the parent rail; standalone\n  usage falls back to `selected`.\n- An item placed after a `udx-navigation-rail-section` only renders while\n  the rail is extended.\n- `badge` puts a `[udxBadge]` on the icon, the way Material shows\n  notifications on a destination; set it back to `undefined` once the\n  destination is selected if the notification is meant to clear, and the\n  badge animates out.\n- The label reveal (width/height + opacity, on `extended` changes) is\n  driven by a shared `@udixio/core/dom` Motion controller, the same one\n  the React adapter uses.",
        "a11y": "- Exposes `aria-current=\"page\"` when selected, since the rail is a\n  navigation landmark rather than a tabbed panel switcher.",
        "limitations": "- No arrow-key navigation between items; relies on the native sequential\n  tab order.\n- `[class.x]` and `[ngClass]` bind to the `display: contents` host and have no visible effect; use `class`, `[class]`, or `classes`."
      },
      "inputs": {
        "label": {
          "name": "label",
          "description": "Text shown alongside (or as the accessible name for) the icon.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "icon": {
          "name": "icon",
          "description": "Icon shown while the item is not selected.",
          "required": false,
          "type": {
            "name": "IconType"
          },
          "defaultValue": null
        },
        "iconSelected": {
          "name": "iconSelected",
          "description": "Icon shown while the item is selected.",
          "required": false,
          "type": {
            "name": "IconType"
          },
          "defaultValue": null
        },
        "badge": {
          "name": "badge",
          "description": "A badge on the icon: `{}` is the dot, `{ label: 3 }` the count.",
          "required": false,
          "type": {
            "name": "BadgeProps"
          },
          "defaultValue": null
        },
        "selected": {
          "name": "selected",
          "description": "Controlled selected state, used when no parent drives the selection.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "false"
          }
        },
        "href": {
          "name": "href",
          "description": "Navigation destination; switches the inner element to a native link.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "hostClass": {
          "name": "hostClass",
          "description": "Classes applied to the root element. Angular's native `class` attribute and `[class]` binding land here, merged with the component's own classes.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": {
            "value": "''"
          },
          "alias": "class"
        },
        "classes": {
          "name": "classes",
          "description": "Static or state-aware classes for the component's internal elements, keyed by element name.",
          "required": false,
          "type": {
            "name": "Partial<Record<\"label\" | \"container\" | \"icon\" | \"stateLayer\" | \"navigationRailItem\", string>> | ClassNameComponent<NavigationRailItemInterface>"
          },
          "defaultValue": null
        }
      },
      "outputs": {}
    }
  }
}
