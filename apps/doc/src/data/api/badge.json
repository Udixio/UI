{
  "schemaVersion": 4,
  "displayName": "Badge",
  "description": "Badges show notifications, counts, or status information on navigation items and icons.",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/Badge.tsx",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- Wrap what the badge marks: `<Badge label={3}><Icon icon={iInbox} /></Badge>`.\n  The wrapper provides the positioned container itself, so the anchoring\n  never depends on a `relative` the caller has to remember.\n- No `label` renders the small dot Material uses for an unread notification;\n  any label renders the large pill. The variant follows from the content, so\n  a small badge carrying a count cannot be expressed.\n- `max` caps a count the way Material spells it: `label={100} max={99}`\n  renders `99+`.\n- Hiding the badge once its destination is selected is the caller's\n  decision: set `visible={false}` rather than unmounting it, so it can\n  animate out. The show/hide scale-and-fade is implemented once with\n  anime.js in `@udixio/core/dom`; `transition` tunes it, and reduced motion\n  skips it.",
        "a11y": "- `description` is what a screen reader announces. Give it the meaning, not\n  the number: `3 unread messages`, not `3`. It is rendered as visually\n  hidden text inside a live region, so a changing count announces the whole\n  sentence rather than the bare digit, and the visible number is hidden from\n  assistive technology so it is not read twice.\n- Without a `description` the badge is hidden from assistive technology\n  rather than announced as a bare digit or as nothing at all; so is a\n  badge that is not `visible`.",
        "limitations": "- Material limits badge content to four characters including the `+`.\n  Nothing truncates: silently dropping a caller's text would hide data, and\n  `max` is the tool for the count case.\n- The badge is positioned against its own wrapper, so it marks whatever it\n  wraps rather than an arbitrary element elsewhere on the page. Material\n  anchors badges inside the *icon* bounding box, so wrap the icon: wrapping a\n  control with a large touch target anchors to that target instead, pushing\n  the badge away from the icon by the padding around it."
      },
      "methods": [],
      "props": {
        "label": {
          "name": "label",
          "description": "Text shown inside the badge. Omitted, the badge renders as the small dot\nMaterial 3 uses for an unread notification.\n\nMaterial limits badge content to four characters including a `+`; use\n`max` to keep a count within that.",
          "required": false,
          "type": {
            "name": "string | number"
          },
          "defaultValue": null
        },
        "max": {
          "name": "max",
          "description": "Caps a numeric `label`. A label of 100 with `max` 99 renders `99+`.\nIgnored when `label` is not a number.",
          "required": false,
          "type": {
            "name": "number"
          },
          "defaultValue": null
        },
        "description": {
          "name": "description",
          "description": "What assistive technology announces for the badge, such as\n`3 unread messages`.\n\nThe visible label is rarely enough on its own -- `3` says nothing about\nwhat there are three of -- and the small dot has no text at all, so this\nis the only way it reaches a screen reader.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "visible": {
          "name": "visible",
          "description": "Whether the badge is shown. Default: `true`.\n\nSet it to `false` rather than unmounting the badge, so it can animate\nout; it stays in the DOM, invisible and hidden from assistive technology,\nuntil it is shown again. Material recommends clearing a badge once its\ndestination has been viewed.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "true"
          }
        },
        "transition": {
          "name": "transition",
          "description": "Anime.js show-hide timing.",
          "required": false,
          "type": {
            "name": "BadgeTransition"
          },
          "defaultValue": null
        },
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<BadgeInterface>"
          },
          "defaultValue": null
        },
        "children": {
          "name": "children",
          "description": "The icon or item the badge is anchored to.",
          "required": false,
          "type": {
            "name": "ReactNode"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/badge/badge.ts",
      "selector": "[udxBadge]",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- Put it on what the badge marks: `<udx-icon [icon]=\"iInbox\" udxBadge=\"3\" />`.\n  The directive places the badge inside the element it sits on and makes\n  that element the containing block, so nothing has to be wrapped and no\n  `relative` has to be remembered. Inputs are prefixed `udxBadge*` because\n  the host element is not the directive's own.\n- The bare attribute renders the small dot Material uses for an unread\n  notification; any label renders the large pill. The variant follows from\n  the content, so a small badge carrying a count cannot be expressed.\n- `udxBadgeMax` caps a count the way Material spells it:\n  `udxBadge=\"100\" udxBadgeMax=\"99\"` renders `99+`.\n- Hiding the badge once its destination is selected is the caller's\n  decision: set `udxBadgeVisible` to `false` rather than removing the\n  attribute, so it can animate out -- or let `udx-navigation-rail-item` do\n  it through its `badge` input. The show/hide scale-and-fade is implemented\n  once with anime.js in `@udixio/core/dom`; `udxBadgeTransition` tunes it,\n  and reduced motion skips it.",
        "a11y": "- `udxBadgeDescription` is what a screen reader announces. Give it the\n  meaning, not the number: `3 unread messages`, not `3`. It is rendered as\n  visually hidden text inside a live region, so a changing count announces\n  the whole sentence rather than the bare digit, and the visible number is\n  hidden from assistive technology so it is not read twice.\n- Without a description the badge is hidden from assistive technology\n  rather than announced as a bare digit or as nothing at all; so is a\n  badge that is not visible.",
        "limitations": "- Material limits badge content to four characters including the `+`.\n  Nothing truncates: silently dropping a caller's text would hide data, and\n  `udxBadgeMax` is the tool for the count case.\n- The badge lives inside the marked element, so that element -- or, for a\n  `display: contents` host such as `udx-icon`, the first box inside it --\n  must accept children. An `<img>` or an `<input>` cannot carry one.\n- Material anchors badges inside the *icon* bounding box, so mark the icon:\n  marking a control with a large touch target anchors to that target\n  instead, pushing the badge away from the icon by the padding around it.\n- The `container` element of the shared style contract is the consumer's\n  own element here, so a `container` override in `udxBadgeClass` has no\n  target; style that element directly."
      },
      "inputs": {
        "label": {
          "name": "label",
          "description": "Text shown inside the badge. Omitted, it renders as the small dot.",
          "required": false,
          "type": {
            "name": "string | number | undefined"
          },
          "defaultValue": null,
          "alias": "udxBadge"
        },
        "max": {
          "name": "max",
          "description": "Caps a numeric label: 100 with a max of 99 renders `99+`.",
          "required": false,
          "type": {
            "name": "number | undefined"
          },
          "defaultValue": null,
          "alias": "udxBadgeMax"
        },
        "description": {
          "name": "description",
          "description": "What assistive technology announces, such as `3 unread messages`.",
          "required": false,
          "type": {
            "name": "string | undefined"
          },
          "defaultValue": null,
          "alias": "udxBadgeDescription"
        },
        "visible": {
          "name": "visible",
          "description": "Whether the badge is shown; `false` animates it out and keeps it mounted.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "true"
          },
          "alias": "udxBadgeVisible"
        },
        "transition": {
          "name": "transition",
          "description": "Anime.js show-hide timing.",
          "required": false,
          "type": {
            "name": "BadgeTransition | undefined"
          },
          "defaultValue": null,
          "alias": "udxBadgeTransition"
        },
        "classes": {
          "name": "classes",
          "description": "Classes, or state-aware element classes, applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<BadgeInterface> | undefined"
          },
          "defaultValue": null,
          "alias": "udxBadgeClass"
        }
      },
      "outputs": {}
    }
  }
}
