{
  "schemaVersion": 2,
  "displayName": "CarouselItem",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/CarouselItem.tsx",
      "description": "A single slide inside a `Carousel`. Its width is driven by the carousel's\nscroll position; it simply projects its children.",
      "tags": {
        "status": "beta",
        "parent": "Carousel",
        "devx": "- Intended for use inside `Carousel`, which injects sizing and slide roles.",
        "a11y": "- Rendered by `Carousel` as a `group` with `aria-roledescription=\"slide\"` and\n  an `aria-label`; used standalone it is a plain container with no slide role.",
        "limitations": "- Sizing (`outputRange`) is provided by the parent `Carousel`; used on its own\n  the item has no min/max width."
      },
      "methods": [],
      "props": {
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<CarouselItemInterface>"
          },
          "defaultValue": null
        },
        "outputRange": {
          "name": "outputRange",
          "description": "Min/max item width in px, provided by the parent `Carousel`.",
          "required": false,
          "type": {
            "name": "[number, number]"
          },
          "defaultValue": null
        }
      }
    }
  }
}
