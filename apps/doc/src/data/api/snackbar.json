{
  "schemaVersion": 2,
  "displayName": "Snackbar",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/Snackbar.tsx",
      "description": "Snackbars show a brief, non-blocking status message about an app process.",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- `open` is controlled; `defaultOpen` initializes uncontrolled usage. Defaults to `true`.\n- `duration` (ms) auto-dismisses the snackbar; omit it to require an explicit `onOpenChange`\n  or a click on the built-in close button.\n- Renders in normal document flow at the call site. Fixed/bottom positioning, stacking, and\n  queueing multiple snackbars are the caller's responsibility.\n- The height transition is implemented once with Motion JavaScript in `@udixio/core/dom`,\n  shared with Angular. It applies the initial `open` state immediately; only transitions\n  after the first render animate.",
        "a11y": "- Renders `role=\"status\"` and `aria-live=\"polite\"` so assistive technology announces the\n  message without interrupting the user. The element stays mounted and `inert` while closed\n  instead of unmounting, so the live region is reliably present before it announces.",
        "limitations": "- No built-in queue/stacking for multiple simultaneous snackbars."
      },
      "methods": [],
      "props": {
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<SnackbarInterface>"
          },
          "defaultValue": null
        },
        "message": {
          "name": "message",
          "description": "Message announced through the snackbar.",
          "required": true,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "open": {
          "name": "open",
          "description": "Controlled open state.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": null
        },
        "defaultOpen": {
          "name": "defaultOpen",
          "description": "Initial open state when uncontrolled. Defaults to `true`.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "true"
          }
        },
        "duration": {
          "name": "duration",
          "description": "Auto-dismiss delay in milliseconds. Omit to require an explicit close.",
          "required": false,
          "type": {
            "name": "number"
          },
          "defaultValue": null
        },
        "closeIcon": {
          "name": "closeIcon",
          "description": "Icon for the built-in close button.",
          "required": false,
          "type": {
            "name": "Icon"
          },
          "defaultValue": {
            "value": "iClose"
          }
        },
        "transition": {
          "name": "transition",
          "description": "Motion transition shared by every framework for the open/close height animation.",
          "required": false,
          "type": {
            "name": "Transition"
          },
          "defaultValue": null
        },
        "onOpenChange": {
          "name": "onOpenChange",
          "description": "Notifies an accepted open-state request.",
          "required": false,
          "type": {
            "name": "(open: boolean) => void"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/snackbar/snackbar.ts",
      "description": "Snackbars show a brief, non-blocking status message about an app process.",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- `open` is controlled; `defaultOpen` initializes uncontrolled usage. Defaults to `true`.\n- `duration` (ms) auto-dismisses the snackbar; omit it to require an explicit `openChange`\n  or a click on the built-in close button.\n- Renders in normal document flow at the call site. Fixed/bottom positioning, stacking, and\n  queueing multiple snackbars are the caller's responsibility.\n- The height transition is implemented once with Motion JavaScript in `@udixio/core/dom`,\n  shared with React. It applies the initial `open` state immediately; only transitions after\n  the first render animate.",
        "a11y": "- Renders `role=\"status\"` and `aria-live=\"polite\"` so assistive technology announces the\n  message without interrupting the user. The element stays mounted and `inert` while closed\n  instead of unmounting, so the live region is reliably present before it announces.",
        "limitations": "- No built-in queue/stacking for multiple simultaneous snackbars."
      },
      "inputs": {
        "message": {
          "name": "message",
          "description": "Message announced through the snackbar.",
          "required": true,
          "type": {
            "name": "string"
          },
          "defaultValue": null
        },
        "open": {
          "name": "open",
          "description": "Controlled open state.",
          "required": false,
          "type": {
            "name": "boolean | undefined"
          },
          "defaultValue": null
        },
        "defaultOpen": {
          "name": "defaultOpen",
          "description": "Initial open state when uncontrolled. Defaults to `true`.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": {
            "value": "true"
          }
        },
        "duration": {
          "name": "duration",
          "description": "Auto-dismiss delay in milliseconds. Omit to require an explicit close.",
          "required": false,
          "type": {
            "name": "number | undefined"
          },
          "defaultValue": null
        },
        "closeIcon": {
          "name": "closeIcon",
          "description": "Icon for the built-in close button.",
          "required": false,
          "type": {
            "name": "string | IconDefinition | SvgImport"
          },
          "defaultValue": {
            "value": "iClose"
          }
        },
        "transition": {
          "name": "transition",
          "description": "Motion transition shared by every framework for the open/close height animation.",
          "required": false,
          "type": {
            "name": "Transition | undefined"
          },
          "defaultValue": null
        },
        "className": {
          "name": "className",
          "description": "Classes or state-aware element classes applied through the shared style contract.",
          "required": false,
          "type": {
            "name": "string | ClassNameComponent<SnackbarInterface>"
          },
          "defaultValue": null
        }
      },
      "outputs": {
        "openChange": {
          "name": "openChange",
          "description": "Emits an accepted open-state request and supports `[(open)]`.",
          "required": false,
          "type": {
            "name": "boolean"
          },
          "defaultValue": null
        }
      }
    }
  }
}
