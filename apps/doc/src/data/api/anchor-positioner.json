{
  "schemaVersion": 5,
  "displayName": "AnchorPositioner",
  "description": "Floats content beside an anchor element and keeps it there as the page moves.",
  "defaultFramework": "react",
  "frameworks": {
    "react": {
      "filePath": "packages/ui-react/src/lib/components/AnchorPositioner.tsx",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- Internal building block for `Tooltip`; not yet documented as a\n  standalone public component.\n- Portals `children` to `document.body`.",
        "a11y": "- Renders no semantics of its own; the caller's content and `Tooltip`'s\n  own `role=\"tooltip\"` carry accessibility meaning.",
        "limitations": "- Falls back to tracking `getBoundingClientRect()` on scroll and resize\n  in browsers without native CSS Anchor Positioning support."
      },
      "methods": [],
      "props": {
        "position": {
          "name": "position",
          "description": "Placement of the floating element relative to its anchor. Defaults to `bottom`.",
          "required": false,
          "type": {
            "name": "AnchorPosition"
          },
          "defaultValue": {
            "value": "'bottom'"
          }
        },
        "anchorRef": {
          "name": "anchorRef",
          "description": "The element the floating content is positioned relative to.",
          "required": true,
          "type": {
            "name": "RefObject<HTMLElement>"
          },
          "defaultValue": null
        }
      }
    },
    "angular": {
      "filePath": "packages/ui-angular/src/lib/anchor-positioner/anchor-positioner.ts",
      "selector": "udx-anchor-positioner",
      "tags": {
        "status": "beta",
        "category": "Communication",
        "devx": "- Internal building block for `Tooltip`; not yet documented as a\n  standalone public component.\n- Portals its content to `document.body`.",
        "a11y": "- Renders no semantics of its own; the caller's content and `Tooltip`'s\n  own `role=\"tooltip\"` carry accessibility meaning.",
        "limitations": "- Falls back to tracking `getBoundingClientRect()` on scroll and resize\n  in browsers without native CSS Anchor Positioning support.\n- `[class.x]` and `[ngClass]` bind to the `display: contents` host and have no visible effect; use `class` or `[class]`."
      },
      "inputs": {
        "anchor": {
          "name": "anchor",
          "description": "The element the floating content is positioned relative to.",
          "required": true,
          "type": {
            "name": "HTMLElement | ElementRef<HTMLElement>"
          },
          "defaultValue": null
        },
        "position": {
          "name": "position",
          "description": "Placement of the floating element relative to its anchor. Defaults to `bottom`.",
          "required": false,
          "type": {
            "name": "AnchorPosition"
          },
          "defaultValue": {
            "value": "'bottom'"
          }
        },
        "hostClass": {
          "name": "hostClass",
          "description": "Classes applied to the root element. Angular's native `class` attribute and `[class]` binding land here, merged with the component's own classes.",
          "required": false,
          "type": {
            "name": "string"
          },
          "defaultValue": {
            "value": "''"
          },
          "alias": "class"
        }
      },
      "outputs": {},
      "content": {
        "default": {
          "name": "default",
          "selector": "*",
          "description": "Default projected content."
        }
      }
    }
  }
}
