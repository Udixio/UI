---
---

import Code from '@/components/Code.astro';
import DatePickerBasicReact from '@/examples/react/date-picker-basic.tsx';
import { DatePickerBasicAngular } from '@/examples/angular/date-picker-basic.ts';
import datePickerBasicReactSource from '@/examples/react/date-picker-basic.tsx?raw';
import datePickerBasicAngularSource from '@/examples/angular/date-picker-basic.ts?raw';
import DatePickerRangeReact from '@/examples/react/date-picker-range.tsx';
import { DatePickerRangeAngular } from '@/examples/angular/date-picker-range.ts';
import datePickerRangeReactSource from '@/examples/react/date-picker-range.tsx?raw';
import datePickerRangeAngularSource from '@/examples/angular/date-picker-range.ts?raw';
import DatePickerUncontrolledReact from '@/examples/react/date-picker-uncontrolled.tsx';
import { DatePickerUncontrolledAngular } from '@/examples/angular/date-picker-uncontrolled.ts';
import datePickerUncontrolledReactSource from '@/examples/react/date-picker-uncontrolled.tsx?raw';
import datePickerUncontrolledAngularSource from '@/examples/angular/date-picker-uncontrolled.ts?raw';
import DatePickerConstraintsReact from '@/examples/react/date-picker-constraints.tsx';
import { DatePickerConstraintsAngular } from '@/examples/angular/date-picker-constraints.ts';
import datePickerConstraintsReactSource from '@/examples/react/date-picker-constraints.tsx?raw';
import datePickerConstraintsAngularSource from '@/examples/angular/date-picker-constraints.ts?raw';
import DatePickerLocalizationReact from '@/examples/react/date-picker-localization.tsx';
import { DatePickerLocalizationAngular } from '@/examples/angular/date-picker-localization.ts';
import datePickerLocalizationReactSource from '@/examples/react/date-picker-localization.tsx?raw';
import datePickerLocalizationAngularSource from '@/examples/angular/date-picker-localization.ts?raw';

## Usage

DatePicker lets people select a date, or a range of dates, from a month grid.
`mode="range"` discriminates `value`/`defaultValue`/`onChange` (React) or the
inputs bound to `value`/`defaultValue`/`valueChange` (Angular) to a
`[start, end]` `DateRange` instead of a single `Date`.

## Controlled selection

Use `value` with `onChange` in React, or `[(value)]` in Angular. A controlled
DatePicker emits a request but does not change until its owner supplies the
new value.

<Code
  codes={{
    react: datePickerBasicReactSource,
    angular: datePickerBasicAngularSource,
  }}
>
  <DatePickerBasicReact slot="react" client:load />
  <DatePickerBasicAngular slot="angular" client:load />
</Code>

## Selecting a range

Set `mode="range"`. The first click starts a new range; the second completes
it in chronological order regardless of click order, and a third click after
completion starts a new range.

<Code
  codes={{
    react: datePickerRangeReactSource,
    angular: datePickerRangeAngularSource,
  }}
>
  <DatePickerRangeReact slot="react" client:load />
  <DatePickerRangeAngular slot="angular" client:load />
</Code>

## Uncontrolled usage

`defaultValue` captures the initial value once; the component then owns its
own selection and only notifies changes.

<Code
  codes={{
    react: datePickerUncontrolledReactSource,
    angular: datePickerUncontrolledAngularSource,
  }}
>
  <DatePickerUncontrolledReact slot="react" client:load />
  <DatePickerUncontrolledAngular slot="angular" client:load />
</Code>

## Constraining selectable dates

`minDate`/`maxDate` and `shouldDisableDate` are compared at day granularity, so
a boundary carrying a time-of-day (for example `new Date()`) never disables
its own day. Blocked days stay visible and focusable, but cannot be selected.

<Code
  codes={{
    react: datePickerConstraintsReactSource,
    angular: datePickerConstraintsAngularSource,
  }}
>
  <DatePickerConstraintsReact slot="react" client:load />
  <DatePickerConstraintsAngular slot="angular" client:load />
</Code>

## Localization

`locale` formats the month label and weekday headers; `weekStartDay` shifts
which day begins each row.

<Code
  codes={{
    react: datePickerLocalizationReactSource,
    angular: datePickerLocalizationAngularSource,
  }}
>
  <DatePickerLocalizationReact slot="react" client:load />
  <DatePickerLocalizationAngular slot="angular" client:load />
</Code>

## Accessibility

The day grid uses the composite `grid`/`row`/`gridcell` pattern with one
roving `tabIndex` per visible month. Arrow keys move focus by day or week,
Home/End jump to the visible week's boundaries, and PageUp/PageDown change
month (Shift for year). The selected day's `gridcell` carries `aria-selected`;
its focusable day button carries `aria-current="date"` when it is today.
Unavailable days stay focusable with `aria-disabled` instead of the native
`disabled` attribute, so keyboard users can still traverse past them.

DatePicker does not render its own text input or popup positioning; combine it
with `TextField`/`IconButton` and your own overlay to build a popup picker.
