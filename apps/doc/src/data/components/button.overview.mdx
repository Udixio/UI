---
---

import { CodePreview } from '@/components/CodePreview.js';
import Code from '@/components/Code.astro';
import ButtonVariantsReact from '@/examples/react/button-variants.tsx';
import ButtonSizesReact from '@/examples/react/button-sizes.tsx';
import ButtonToggleReact from '@/examples/react/button-toggle.tsx';
import { ButtonVariantsAngular } from '@/examples/angular/button-variants.ts';
import { ButtonToggleAngular } from '@/examples/angular/button-toggle.ts';
import buttonVariantsReactSource from '@/examples/react/button-variants.tsx?raw';
import buttonSizesReactSource from '@/examples/react/button-sizes.tsx?raw';
import buttonToggleReactSource from '@/examples/react/button-toggle.tsx?raw';
import buttonVariantsAngularSource from '@/examples/angular/button-variants.ts?raw';
import buttonToggleAngularSource from '@/examples/angular/button-toggle.ts?raw';
import { iAdd } from '@udixio/icons-rounded-400/add';

## Usage

The Button component can trigger an action or navigate to a link. Import it from the @udixio/ui-react package and provide at least a label via the label prop.

```bash
import { Button } from "@udixio/ui-react"
```

```tsx
<Button>Button</Button>
```

## Variants

<Code
  codes={{
    react: buttonVariantsReactSource,
    angular: buttonVariantsAngularSource,
  }}
>
  <ButtonVariantsReact slot="react" client:load />
  <ButtonVariantsAngular slot="angular" client:load />
</Code>

## Toggle buttons

Use `toggleable` for the semantic mode. `pressed` is the controlled value and
`defaultPressed` initializes an uncontrolled button. React notifies changes with
`onPressedChange`; Angular exposes the matching `pressedChange` output.

<Code
  codes={{
    react: buttonToggleReactSource,
    angular: buttonToggleAngularSource,
  }}
>
  <ButtonToggleReact slot="react" client:load />
  <ButtonToggleAngular slot="angular" client:load />
</Code>

The component manages `aria-pressed` automatically in both frameworks.

## Disabled state

Use the `disabled` prop to make the button inactive.

```jsx
<div className="flex flex-wrap gap-3">
  <Button label="Disabled" disabled />
  <Button label="Disabled" variant="outlined" disabled />
</div>
```

## Sizes

<Code codes={{ react: buttonSizesReactSource }}>
  <ButtonSizesReact slot="react" client:load />
</Code>

## Shapes

Control the button's appearance via the `shape` prop.

```jsx
<div className="flex flex-wrap gap-3">
  <Button label="Rounded" shape="rounded" />
  <Button label="Squared" shape="squared" />
</div>
```

## Icon

You can add an icon (FontAwesome) and choose its position.

<CodePreview
  client:load
  code={`
    <div className="flex gap-3">
      <Button label="Add" icon={iAdd} />
      <Button label="Next" iconPosition="right" icon={iAdd} />
    </div>
  `}
  scope={{ iAdd }}
/>

## Loading state

```jsx
<Button label="Sending..." loading />
```

## Link or action

If the `href` prop is provided, the component renders an `<a>` link; otherwise it renders a `<button>`.

```jsx
<div className="flex gap-3">
  <Button label="Click me" onClick={() => console.log('clicked')} />
  <Button label="Go to Udixio" href="https://udixio.dev" />
</div>
```
