---
---

import Code from '@/components/Code.astro';
import ButtonVariantsReact from '@/examples/react/button-variants.tsx';
import ButtonSizesReact from '@/examples/react/button-sizes.tsx';
import ButtonToggleReact from '@/examples/react/button-toggle.tsx';
import ButtonStatesReact from '@/examples/react/button-states.tsx';
import ButtonIconsReact from '@/examples/react/button-icons.tsx';
import ButtonActionsReact from '@/examples/react/button-actions.tsx';
import { ButtonVariantsAngular } from '@/examples/angular/button-variants.ts';
import { ButtonSizesAngular } from '@/examples/angular/button-sizes.ts';
import { ButtonToggleAngular } from '@/examples/angular/button-toggle.ts';
import { ButtonStatesAngular } from '@/examples/angular/button-states.ts';
import { ButtonIconsAngular } from '@/examples/angular/button-icons.ts';
import { ButtonActionsAngular } from '@/examples/angular/button-actions.ts';
import buttonVariantsReactSource from '@/examples/react/button-variants.tsx?raw';
import buttonSizesReactSource from '@/examples/react/button-sizes.tsx?raw';
import buttonToggleReactSource from '@/examples/react/button-toggle.tsx?raw';
import buttonStatesReactSource from '@/examples/react/button-states.tsx?raw';
import buttonIconsReactSource from '@/examples/react/button-icons.tsx?raw';
import buttonActionsReactSource from '@/examples/react/button-actions.tsx?raw';
import buttonVariantsAngularSource from '@/examples/angular/button-variants.ts?raw';
import buttonSizesAngularSource from '@/examples/angular/button-sizes.ts?raw';
import buttonToggleAngularSource from '@/examples/angular/button-toggle.ts?raw';
import buttonStatesAngularSource from '@/examples/angular/button-states.ts?raw';
import buttonIconsAngularSource from '@/examples/angular/button-icons.ts?raw';
import buttonActionsAngularSource from '@/examples/angular/button-actions.ts?raw';

## Usage

Button triggers an action or navigates to a destination. Import `Button` from
`@udixio/ui-react` or `@udixio/ui-angular`. Provide visible text with React
children, Angular projected content, or the shared `label` property. React
accepts exactly one of `label` and `children`, which keeps the visible and
accessible labels from contradicting each other.

Action buttons render a native `<button>` and default to `type="button"`.
Providing `href` renders a native `<a>` instead.

## Variants

The canonical variants are `filled`, `elevated`, `tonal`, `outlined`, and
`text`. The compatibility aliases `primary` and `secondary` resolve to `filled`
and `tonal`.

<Code
  codes={{
    react: buttonVariantsReactSource,
    angular: buttonVariantsAngularSource,
  }}
>
  <ButtonVariantsReact slot="react" client:load />
  <ButtonVariantsAngular slot="angular" client:load />
</Code>

## Toggle buttons

`toggleable` enables toggle-button semantics. `pressed` is the controlled value;
`defaultPressed` is captured once for uncontrolled usage. React emits
`onPressedChange` and Angular emits `pressedChange`. `onClick` and `(click)`
remain independent action events.

<Code
  codes={{
    react: buttonToggleReactSource,
    angular: buttonToggleAngularSource,
  }}
>
  <ButtonToggleReact slot="react" client:load />
  <ButtonToggleAngular slot="angular" client:load />
</Code>

The visible label stays stable while `aria-pressed` communicates the state.
Navigation links ignore toggle state; mark the current destination with
`aria-current` instead.

## Sizes

Sizes range from `xSmall` to `xLarge`. Every size includes a 48px pointer target,
including compact visual treatments.

<Code
  codes={{ react: buttonSizesReactSource, angular: buttonSizesAngularSource }}
>
  <ButtonSizesReact slot="react" client:load />
  <ButtonSizesAngular slot="angular" client:load />
</Code>

## States and shapes

`disabled` and `loading` block actions and pressed-state changes. Loading also
sets `aria-busy` while keeping the label available as the accessible name.
`shape` selects the rounded or squared resting shape.

<Code
  codes={{ react: buttonStatesReactSource, angular: buttonStatesAngularSource }}
>
  <ButtonStatesReact slot="react" client:load />
  <ButtonStatesAngular slot="angular" client:load />
</Code>

By default, the shared Motion controller transforms the button shape on press.
Set `shapeFeedback="none"` in React or Angular to keep a static shape, or pass a
Motion `transition` to customize the `morph` feedback. Reduced-motion
preferences keep the feedback but remove the movement.

Text buttons keep their full layout box by default. Set `edgeAligned` /
`[edgeAligned]="true"` when the button should align to the surrounding inline
edge with negative inline margins.

## Icons

`iconPosition` uses the logical values `start` and `end`, so placement follows
the page direction in RTL layouts. The historical `left` and `right` aliases
remain supported. Button icons are decorative; the text label names the action.

<Code
  codes={{ react: buttonIconsReactSource, angular: buttonIconsAngularSource }}
>
  <ButtonIconsReact slot="react" client:load />
  <ButtonIconsAngular slot="angular" client:load />
</Code>

## Link or action

React forwards native button or anchor attributes. Angular exposes the common
link attributes `target`, `rel`, `tabIndex`, `aria-label`, `aria-describedby`,
and `aria-current` on the inner interactive element. A disabled or loading link
has no `href`, receives `aria-disabled="true"`, and leaves the tab order.

<Code
  codes={{
    react: buttonActionsReactSource,
    angular: buttonActionsAngularSource,
  }}
>
  <ButtonActionsReact slot="react" client:load />
  <ButtonActionsAngular slot="angular" client:load />
</Code>

## Accessibility

Button relies on native button and link keyboard behavior. Toggle buttons expose
`aria-pressed`; loading buttons expose `aria-busy`; and keyboard focus receives
a visible two-pixel outline. Always provide a concise accessible name. When
custom visual content has no accessible text, provide `aria-label` in both
frameworks. Never provide a label that contradicts visible text.

## Migration to the next major

Button's stable contract intentionally has no compatibility aliases. Update
consumers before adopting the next major release:

| Previous API                           | Stable API                                            |
| -------------------------------------- | ----------------------------------------------------- |
| `disableTextMargins`                   | `edgeAligned` with inverted polarity                  |
| `allowShapeTransformation={false}`     | `shapeFeedback="none"`                                |
| `activated`                            | `pressed` or `defaultPressed`                         |
| `onToggle`                             | `toggleable` plus `onPressedChange` / `pressedChange` |
| React `label` together with `children` | Choose one; add `aria-label` for non-text children    |

`edgeAligned` defaults to `false` and `shapeFeedback` to `morph`. Toggle state
now follows the controlled and uncontrolled contract described above, and
action events remain independent.
