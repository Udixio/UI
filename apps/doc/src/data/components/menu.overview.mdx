---
---

import Code from '@/components/Code.astro';
import MenuFamilyReact from '@/examples/react/menu-family.tsx';
import { MenuFamilyAngular } from '@/examples/angular/menu-family.ts';
import menuFamilyReactSource from '@/examples/react/menu-family.tsx?raw';
import menuFamilyAngularSource from '@/examples/angular/menu-family.ts?raw';
import MenuVariantsReact from '@/examples/react/menu-variants.tsx';
import { MenuVariantsAngular } from '@/examples/angular/menu-variants.ts';
import menuVariantsReactSource from '@/examples/react/menu-variants.tsx?raw';
import menuVariantsAngularSource from '@/examples/angular/menu-variants.ts?raw';
import MenuSelectionReact from '@/examples/react/menu-selection.tsx';
import { MenuSelectionAngular } from '@/examples/angular/menu-selection.ts';
import menuSelectionReactSource from '@/examples/react/menu-selection.tsx?raw';
import menuSelectionAngularSource from '@/examples/angular/menu-selection.ts?raw';
import MenuLinksScrollReact from '@/examples/react/menu-links-scroll.tsx';
import { MenuLinksScrollAngular } from '@/examples/angular/menu-links-scroll.ts';
import menuLinksScrollReactSource from '@/examples/react/menu-links-scroll.tsx?raw';
import menuLinksScrollAngularSource from '@/examples/angular/menu-links-scroll.ts?raw';
import MenuSelectFieldReact from '@/examples/react/menu-select-field.tsx';
import { MenuSelectFieldAngular } from '@/examples/angular/menu-select-field.ts';
import menuSelectFieldReactSource from '@/examples/react/menu-select-field.tsx?raw';
import menuSelectFieldAngularSource from '@/examples/angular/menu-select-field.ts?raw';
import ContextMenuReact from '@/examples/react/context-menu.tsx';
import { ContextMenuAngular } from '@/examples/angular/context-menu.ts';
import contextMenuReactSource from '@/examples/react/context-menu.tsx?raw';
import contextMenuAngularSource from '@/examples/angular/context-menu.ts?raw';

## One family, one documentation

`Menu`, `MenuItem`, `MenuGroup`, `MenuHeadline` and `ContextMenu` form a
single family. This page is their shared documentation; each component keeps
a dedicated API reference:

- [`Menu`](/components/menu/api)
- [`MenuItem`](/components/menu-item/api)
- [`MenuGroup`](/components/menu-group/api)
- [`MenuHeadline`](/components/menu-headline/api)
- [`ContextMenu`](/components/context-menu/api)

Both adapters use the same core contract, the same styles and the same
keyboard navigation engine.

## Choosing the right semantics

Use `purpose="actions"` (the default) for commands. The container then
exposes `menu` and its items expose `menuitem`, `menuitemradio` or
`menuitemcheckbox`.

Use `purpose="selection"` for a list of options. The container becomes a
`listbox`, the items become `option`s and their state is announced with
`aria-selected`.

`accessibleLabel` names the menu when no external label is linked to it.

## Composition

- `MenuItem` represents a command or a choice.
- `MenuGroup` groups items and its `label` names the group.
- `MenuHeadline` is a non-interactive visual marker. It does not replace the
  semantic label of a `MenuGroup`. In a menu made of groups, place it inside
  the relevant `MenuGroup` so it stays within its surface.
- `ContextMenu` opens an actions menu at the pointer position or via
  `Shift+F10`.

<Code
  codes={{
    react: menuFamilyReactSource,
    angular: menuFamilyAngularSource,
  }}
>
  <MenuFamilyReact slot="react" client:load />
  <MenuFamilyAngular slot="angular" client:load />
</Code>

## Variants, icons and states

`variant="standard"` uses the neutral surface. `variant="vibrant"` applies
the family color to the whole composition. Leading and trailing icons remain
optional; a disabled item keeps its structure but receives no interaction or
state layer.

When there is no `MenuGroup`, a `MenuHeadline` can be placed directly inside
`Menu`.

<Code
  codes={{
    react: menuVariantsReactSource,
    angular: menuVariantsAngularSource,
  }}
>
  <MenuVariantsReact slot="react" client:load />
  <MenuVariantsAngular slot="angular" client:load />
</Code>

## Controlled and uncontrolled selection

A selectable item declares `selectionType="single"` or
`selectionType="multiple"`.

- In React, control it with `selected` and `onSelectedChange`.
- In Angular, use `selected` and `selectedChange`, including with
  `[(selected)]`.
- `defaultSelected` initializes an uncontrolled item.
- An already selected `single` item does not deselect itself; the group's
  owner picks the new value.

The former names `activated` and `onToggle` were removed: they mixed resolved
state with the event, and did not distinguish single from multiple selection.

The following example brings both models together:

- an actions menu with radio choices (`single`) and checkboxes
  (`multiple`);
- a list of options with `purpose="selection"`;
- controlled states and one state initialized with `defaultSelected`.

<Code
  codes={{
    react: menuSelectionReactSource,
    angular: menuSelectionAngularSource,
  }}
>
  <MenuSelectionReact slot="react" client:load />
  <MenuSelectionAngular slot="angular" client:load />
</Code>

## Links and scrollable content

With `href`, a `MenuItem` becomes a native link. A disabled link loses its
destination and leaves the tab order. When the content exceeds the menu's
maximum height, the surface becomes scrollable without changing keyboard
navigation.

<Code
  codes={{
    react: menuLinksScrollReactSource,
    angular: menuLinksScrollAngularSource,
  }}
>
  <MenuLinksScrollReact slot="react" client:load />
  <MenuLinksScrollAngular slot="angular" client:load />
</Code>

## Menu exposed in a select field

`TextField` with `type="select"` opens a `purpose="selection"` surface,
controls the selected value and closes the menu after the choice. Both
adapters share this behavior; only the way options are declared differs:

- in React, compose `MenuItem` children directly, or pass `options`;
- in Angular, `options` is the only way to populate the menu: the adapter does
  not project custom `MenuItem`s.

<Code
  codes={{
    react: menuSelectFieldReactSource,
    angular: menuSelectFieldAngularSource,
  }}
>
  <MenuSelectFieldReact slot="react" client:load />
  <MenuSelectFieldAngular slot="angular" client:load />
</Code>

## Context menu

The context menu opens on secondary click or with `Shift+F10`. It moves
focus to the first available item. `Escape` closes the popup and restores
focus to the trigger; an outside click or a scroll also closes it.

<Code
  codes={{
    react: contextMenuReactSource,
    angular: contextMenuAngularSource,
  }}
>
  <ContextMenuReact slot="react" client:load />
  <ContextMenuAngular slot="angular" client:load />
</Code>

## Keyboard navigation

In an open menu:

- Arrow down/up moves focus and wraps at both ends.
- Home/End goes to the first/last available item.
- Typing a character searches for the next matching label.
- Disabled items are skipped.
