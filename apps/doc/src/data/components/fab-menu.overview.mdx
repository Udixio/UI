---
---

import Code from '@/components/Code.astro';
import FabMenuActionsReact from '@/examples/react/fab-menu-actions.tsx';
import { FabMenuActionsAngular } from '@/examples/angular/fab-menu-actions.ts';
import fabMenuActionsReactSource from '@/examples/react/fab-menu-actions.tsx?raw';
import fabMenuActionsAngularSource from '@/examples/angular/fab-menu-actions.ts?raw';

## Usage

`FabMenu` reveals a short group of related primary actions from one floating
action button. Its `actions` model is shared by React and Angular.

<Code
  codes={{
    react: fabMenuActionsReactSource,
    angular: fabMenuActionsAngularSource,
  }}
>
  <FabMenuActionsReact slot="react" client:load />
  <FabMenuActionsAngular slot="angular" client:load />
</Code>

## State and action events

Use `open` with `onOpenChange` (React) or `openChange` (Angular) for controlled
state. `defaultOpen` initializes uncontrolled state. Selection emits
`onActionSelect(action, index)` in React and `actionSelect` with
`{ action, index }` in Angular before closing.

Actions may be buttons or links and may include an icon or disabled state.

## Accessibility and interaction

The trigger exposes `aria-expanded` and `aria-controls`. Opening moves focus to
the first enabled action. Escape closes the group and restores trigger focus;
selection and outside press also close it. Actions retain ordinary button/link
semantics inside a labelled group instead of claiming full ARIA menu behavior.

Use `closeLabel` and `actionsLabel` when the generated English defaults are not
appropriate for the product language.

## Migrating to 3.0

| Before                                  | 3.0                                              |
| --------------------------------------- | ------------------------------------------------ |
| `<FabMenu><Button>…</Button></FabMenu>` | `<FabMenu actions={[…]} />`                      |
| child callbacks                         | `onActionSelect(action, index)` / `actionSelect` |
| React-only transition choreography      | shared focus and dismissal behavior              |
| explicit close child                    | `closeIcon` and `closeLabel`                     |
