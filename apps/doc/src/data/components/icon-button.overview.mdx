---
---

import Code from '@/components/Code.astro';
import IconButtonStatesReact from '@/examples/react/icon-button-states.tsx';
import { IconButtonStatesAngular } from '@/examples/angular/icon-button-states.ts';
import iconButtonStatesReactSource from '@/examples/react/icon-button-states.tsx?raw';
import iconButtonStatesAngularSource from '@/examples/angular/icon-button-states.ts?raw';

## Usage

`IconButton` represents a frequent action with one unambiguous icon. Import it
from `@udixio/ui-react` or `@udixio/ui-angular`. `label` and `icon` are required:
the icon stays decorative while the stable label names the native control.

<Code
  codes={{
    react: iconButtonStatesReactSource,
    angular: iconButtonStatesAngularSource,
  }}
>
  <IconButtonStatesReact slot="react" client:load />
  <IconButtonStatesAngular slot="angular" client:load />
</Code>

## Variants, sizes, and shape

Variants are `standard`, `filled`, `tonal`, and `outlined`. Sizes range from
`xSmall` to `xLarge`; widths are `narrow`, `default`, and `wide`. Every
combination retains at least a 48 px target.

`shape` selects the rounded or squared resting shape. The shared Motion
controller morphs the shape on accepted presses by default. Set
`shapeFeedback="none"` to keep it static.

## Toggle buttons

Set `toggleable` for binary actions such as Favorite. `pressed` is controlled;
`defaultPressed` initializes uncontrolled state. React emits
`onPressedChange`; Angular emits `pressedChange`. `pressedIcon` optionally
replaces the resting icon while pressed.

Navigation links ignore toggle state. Use `aria-current` for the current
destination instead.

## Accessibility

Action mode renders a native button with `type="button"` by default. `href`
switches to a native link. Disabled links lose their destination, receive
`aria-disabled`, and leave the tab order. Focus is visible and icons are
decorative.

No tooltip is created implicitly. Compose `Tooltip` explicitly when visual
hover/focus help is useful.

## Migrating to 3.0

| Before                                | 3.0                                                   |
| ------------------------------------- | ----------------------------------------------------- |
| `iconSelected`                        | `pressedIcon`                                         |
| `activated`                           | `pressed` or `defaultPressed`                         |
| `onToggle`                            | `toggleable` plus `onPressedChange` / `pressedChange` |
| `allowShapeTransformation={false}`    | `shapeFeedback="none"`                                |
| text `children` as the label          | required `label` and `icon`                           |
| implicit tooltip from `title`/`label` | compose `Tooltip` explicitly                          |
