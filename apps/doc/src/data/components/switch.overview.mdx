---
---

import Code from '@/components/Code.astro';
import SwitchStatesReact from '@/examples/react/switch-states.tsx';
import { SwitchStatesAngular } from '@/examples/angular/switch-states.ts';
import SwitchIconsReact from '@/examples/react/switch-icons.tsx';
import { SwitchIconsAngular } from '@/examples/angular/switch-icons.ts';
import SwitchStatesSvelte from '@/examples/svelte/switch-states.svelte';
import SwitchIconsSvelte from '@/examples/svelte/switch-icons.svelte';
import switchStatesReactSource from '@/examples/react/switch-states.tsx?raw';
import switchStatesAngularSource from '@/examples/angular/switch-states.ts?raw';
import switchIconsReactSource from '@/examples/react/switch-icons.tsx?raw';
import switchIconsAngularSource from '@/examples/angular/switch-icons.ts?raw';
import switchStatesSvelteSource from '@/examples/svelte/switch-states.svelte?raw';
import switchIconsSvelteSource from '@/examples/svelte/switch-icons.svelte?raw';

## Usage

Switch toggles a single setting on or off. It renders `role="switch"`, not a
native form control, so associate an accessible name with `aria-label` or
`aria-labelledby`.

## Checked and disabled states

Use `checked` with `onCheckedChange` in React, `[(checked)]` in Angular, or
`bind:checked` in Svelte.
`defaultChecked` captures the initial uncontrolled state. A controlled switch
emits a request but does not change until its owner supplies the new value.
Use `disabled` to prevent interaction.

<Code
  codes={{
    react: switchStatesReactSource,
    angular: switchStatesAngularSource,
    svelte: switchStatesSvelteSource,
  }}
>
  <SwitchStatesReact slot="react" client:load />
  <SwitchStatesAngular slot="angular" client:load />
  <SwitchStatesSvelte slot="svelte" client:load />
</Code>

## With icons

Provide `activeIcon` and/or `inactiveIcon` to display inside the thumb. The
thumb slide between positions is driven by a shared `@udixio/core/dom` controller,
so React, Angular and Svelte animate identically.

<Code
  codes={{
    react: switchIconsReactSource,
    angular: switchIconsAngularSource,
    svelte: switchIconsSvelteSource,
  }}
>
  <SwitchIconsReact slot="react" client:load />
  <SwitchIconsAngular slot="angular" client:load />
  <SwitchIconsSvelte slot="svelte" client:load />
</Code>

## Accessibility

Switch exposes `aria-checked` and toggles with a click or with Space/Enter
when focused. It never renders a visible label on its own; pair it with
`aria-label`, `aria-labelledby`, or a visible label element referencing it
through your own markup.
