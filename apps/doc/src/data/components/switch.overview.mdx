---
---





import { CodePreview } from '@/components/CodePreview.js';
import { iDarkMode } from '@udixio/icons-rounded-400/dark_mode';
import { iLightMode } from '@udixio/icons-rounded-400/light_mode';

## Usage

Switch toggles a single setting on or off.

```bash
import { Switch } from "@udixio/ui-react"
```

```tsx
<Switch selected={false} onChange={(v) => console.log('selected:', v)} />
```

## With icons

Provide `activeIcon` and/or `inactiveIcon` to display inside the thumb.

<CodePreview client:load
             code={`
  <div className="flex items-center gap-6">
    <Switch selected inactiveIcon={iDarkMode} activeIcon={iLightMode} />
    <Switch inactiveIcon={iDarkMode} activeIcon={iLightMode} />
  </div>
`}
             scope={{ iDarkMode, iLightMode }}
/>

## Disabled

```jsx
<Switch disabled selected />
```
