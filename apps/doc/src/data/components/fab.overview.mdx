---
---

import Code from '@/components/Code.astro';
import FabVariantsReact from '@/examples/react/fab-variants.tsx';
import FabSizesReact from '@/examples/react/fab-sizes.tsx';
import FabExtendedReact from '@/examples/react/fab-extended.tsx';
import FabActionsReact from '@/examples/react/fab-actions.tsx';
import FabStatesReact from '@/examples/react/fab-states.tsx';
import { FabVariantsAngular } from '@/examples/angular/fab-variants.ts';
import { FabSizesAngular } from '@/examples/angular/fab-sizes.ts';
import { FabExtendedAngular } from '@/examples/angular/fab-extended.ts';
import { FabActionsAngular } from '@/examples/angular/fab-actions.ts';
import { FabStatesAngular } from '@/examples/angular/fab-states.ts';
import FabVariantsSvelte from '@/examples/svelte/fab-variants.svelte';
import FabSizesSvelte from '@/examples/svelte/fab-sizes.svelte';
import FabExtendedSvelte from '@/examples/svelte/fab-extended.svelte';
import FabActionsSvelte from '@/examples/svelte/fab-actions.svelte';
import FabStatesSvelte from '@/examples/svelte/fab-states.svelte';
import fabVariantsReactSource from '@/examples/react/fab-variants.tsx?raw';
import fabSizesReactSource from '@/examples/react/fab-sizes.tsx?raw';
import fabExtendedReactSource from '@/examples/react/fab-extended.tsx?raw';
import fabActionsReactSource from '@/examples/react/fab-actions.tsx?raw';
import fabStatesReactSource from '@/examples/react/fab-states.tsx?raw';
import fabVariantsAngularSource from '@/examples/angular/fab-variants.ts?raw';
import fabSizesAngularSource from '@/examples/angular/fab-sizes.ts?raw';
import fabExtendedAngularSource from '@/examples/angular/fab-extended.ts?raw';
import fabActionsAngularSource from '@/examples/angular/fab-actions.ts?raw';
import fabStatesAngularSource from '@/examples/angular/fab-states.ts?raw';
import fabVariantsSvelteSource from '@/examples/svelte/fab-variants.svelte?raw';
import fabSizesSvelteSource from '@/examples/svelte/fab-sizes.svelte?raw';
import fabExtendedSvelteSource from '@/examples/svelte/fab-extended.svelte?raw';
import fabActionsSvelteSource from '@/examples/svelte/fab-actions.svelte?raw';
import fabStatesSvelteSource from '@/examples/svelte/fab-states.svelte?raw';

## Usage

`Fab` represents the single primary action on a screen. Import it from
`@udixio/ui-react`, `@udixio/ui-angular` or `@udixio/ui-svelte`; `label` and
`icon` are both required, and arbitrary children are not accepted.

Positioning is owned by the surrounding layout: the component never places
itself. Each size keeps a 48 px touch target and a visible focus treatment.

## Color families

Six treatments are available: `primary`, `secondary` and `tertiary`, plus their
`primaryContainer`, `secondaryContainer` and `tertiaryContainer` counterparts.
The family is independent of every other property — any of them can be compact
or extended, at any size.

<Code
  codes={{
    react: fabVariantsReactSource,
    angular: fabVariantsAngularSource,
    svelte: fabVariantsSvelteSource,
  }}
>
  <FabVariantsReact slot="react" client:load />
  <FabVariantsAngular slot="angular" client:load />
  <FabVariantsSvelte slot="svelte" client:load />
</Code>

## Sizes

`small`, `medium` (the default) and `large` give 56, 80 and 96 px containers
with 24, 28 and 36 px icons. A compact fab is always square; an extended one
keeps the height of its size and takes its width from the label.

<Code
  codes={{
    react: fabSizesReactSource,
    angular: fabSizesAngularSource,
    svelte: fabSizesSvelteSource,
  }}
>
  <FabSizesReact slot="react" client:load />
  <FabSizesAngular slot="angular" client:load />
  <FabSizesSvelte slot="svelte" client:load />
</Code>

## Extended and compact

`extended` reveals the label beside the icon. Toggling it animates the pill open
and closed: a shared controller diffs the label's width and opacity with
Anime.js Layout while the container's padding and gap transition alongside it,
on the same duration and easing. Under `prefers-reduced-motion` the change is
applied instantly instead.

In compact mode, `label` remains the accessible name without creating visible
text. The label element stays mounted, collapsed to zero width and hidden from
assistive technology, so the transition never has to unmount anything.

A compact fab also names its icon in a tooltip on hover and focus, which
Material 3 requires of every fab on the web — you get it without asking. Since
that text repeats the accessible name, the tooltip does not also describe the
target, so a screen reader announces it once. `tooltip` takes over when the
hint should say more than the label (`tooltip="Create a note (N)"`, which then
does describe the target), and `tooltip={false}` removes it — an accessibility
exception worth justifying. An extended fab never shows one: its label is
already visible.

<Code
  codes={{
    react: fabExtendedReactSource,
    angular: fabExtendedAngularSource,
    svelte: fabExtendedSvelteSource,
  }}
>
  <FabExtendedReact slot="react" client:load />
  <FabExtendedAngular slot="angular" client:load />
  <FabExtendedSvelte slot="svelte" client:load />
</Code>

## Link or action

Without `href`, the component renders a native button and defaults to
`type="button"`, so it never submits a surrounding form by accident. With
`href`, it renders a native link, where `aria-current` marks the destination
already open.

<Code
  codes={{
    react: fabActionsReactSource,
    angular: fabActionsAngularSource,
    svelte: fabActionsSvelteSource,
  }}
>
  <FabActionsReact slot="react" client:load />
  <FabActionsAngular slot="angular" client:load />
  <FabActionsSvelte slot="svelte" client:load />
</Code>

## Disabled

`disabled` applies to both renderings. A disabled button uses the native
attribute; a disabled link drops its `href`, carries `aria-disabled`, leaves
the tab order, and swallows clicks — a native anchor has no disabled state of
its own.

<Code
  codes={{
    react: fabStatesReactSource,
    angular: fabStatesAngularSource,
    svelte: fabStatesSvelteSource,
  }}
>
  <FabStatesReact slot="react" client:load />
  <FabStatesAngular slot="angular" client:load />
  <FabStatesSvelte slot="svelte" client:load />
</Code>

## Migrating to 3.0

| Before                         | 3.0                               |
| ------------------------------ | --------------------------------- |
| text `children` as the label   | required `label`                  |
| implicit tooltip               | compose `Tooltip` explicitly      |
| missing action type            | `type="button"` by default        |
| documented `variant="surface"` | use a supported container variant |
