{
  "name": "udixio-ui-components",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "nx": "nx",
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "storybook": "nx run ui-react:storybook",
    "build-storybook": "nx run ui-react:build-storybook"
  },
  "devDependencies": {
    "@nrwl/cli": "^15.0.0",
    "@nrwl/nx-cloud": "latest",
    "@nrwl/workspace": "^15.0.0",
    "nx": "21.2.2"
  }
}