# shareable

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shareable` to build the library.

## Running unit tests

Run `nx test shareable` to execute the unit tests via [Jest](https://jestjs.io).
